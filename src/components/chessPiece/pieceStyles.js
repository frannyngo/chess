import styled from "styled-components";

export const PieceContainer = styled.div`
    position: absolute;
    // top: ${props => `${props.y}px` || '0px'};
    // left: ${props => `${props.x}px` || '0px'};
`
// y - top
// x - left